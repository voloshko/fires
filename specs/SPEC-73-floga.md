# SPEC-73: Горы: FLOGA как третий биом стенда

Status: implemented

Requirement: REQ-008

Depends on: SPEC-55, SPEC-69

## Summary

Третий биом стенда SPEC-55 — **средиземноморские горы**. Для заказчика это главный
случай: Кипр, Греция, Испания, крутые склоны, где пожар тушат только с воздуха.
FLOGA (Греция, 2017–2021, 326 событий, контуры Пожарной службы Греции, экспертная
разметка, MIT/CC-BY) целиком весит 1.1 ТБ. Поэтому берём только **контуры**
(`Orion-AI-Lab/FLOGA-annotations`, polygons/v2) и собираем окна тем же рецептом, что
SPEC-72, на HLS v2 из Planetary Computer. Метки у FLOGA экспертные, чище MTBS.

**Сборка (записана до загрузки):**
1. Годы **2020–2021**: `hls2-s30` в Planetary Computer начинается с 2020-01-01.
2. Снимок `hls2-s30`, покрывающий центроид контура, с датой в пределах ±10 суток от
   даты их снимка после пожара (`S2_e`). Если такого нет — от 1 до 60 суток после
   конца пожара (`End date`). Из кандидатов берётся снимок с наименьшей долей облака,
   тени и нодаты в окне, при равенстве — ближайший к `S2_e`; окно отбрасывается при
   доле ≥ 10 %.
3. Окно 512 × 512, 30 м, центр — центроид контура. Крупные пожары (Эвбея 2021)
   попадают в окно частично, это нормально.
4. Маска: 1 внутри контура события, −1 внутри других контуров того же года
   (у них это класс 2), −1 под облаком, тенью и нодатой, иначе 0.
5. Уклон по Copernicus DEM GLO-30 (`cop-dem-glo-30` из Planetary Computer)
   пересчитывается на сетку окна, только для разбивки.
6. Все годные события, без выборки. Манифест с хешами.

**Модели** (нулевой выстрел, никто не видел Грецию с метками): C1 (SPEC-72),
C1-F (SPEC-75), Prithvi, смесь с весом из SPEC-74.

**Результат — строка стенда «горы»**, порогов принятия нет: IoU гари при 0.5 и
95 % бутстреп по окнам для каждой модели. Главная разбивка — **по уклону пикселя**:
< 10°, 10–20°, 20–30°, > 30°. Отвечает на вопрос, теряем ли мы гарь на крутых
склонах. Пример этого на снимке: тень от рельефа на склонах, развёрнутых от солнца.

Не входит: Кипр и Испания. Контуры EFFIS выдаются по заявке, открытых контуров для
них мы пока не нашли. Если найдутся, это будет отдельная спека по тому же рецепту.

## Acceptance Criteria

- `scripts/build_floga_hls.py` → `external/floga_hls/` (`*_merged.tif`, `*.mask.tif`, `*.slope.tif`, `manifest.json`).
- `scripts/exp_mountains.py` печатает таблицу моделей и разбивку по уклону.

## Verification

- На k8plus: сборка и замер; receipt в `evidence/receipts/SPEC-73/`; строка «горы» в REQ-008 и SPEC-55.

## Resolution

Receipt `evidence/receipts/SPEC-73/SPEC-73-FLOGA-001.json`. 186 окон из 191 события
(Греция, 2020–2021, HLS v2); доля гари 2.2 %, медиана уклона гари 14.9°.

| модель | IoU гари при 0.5 | 95 % |
|---|---|---|
| **C1-F (лес + свежий MTBS)** | **0.559** | [0.359, 0.741] |
| C1 (лес HLS) | 0.457 | [0.282, 0.636] |
| смесь пополам | 0.343 | [0.202, 0.494] |
| смесь SPEC-74 (w = 0.3) | 0.168 | [0.094, 0.266] |
| Prithvi | 0.149 | [0.082, 0.238] |

C1 − Prithvi = +0.308, [+0.174, +0.446] → **лучше**, значимо.

По уклону пикселя (IoU гари):

| уклон | C1 | C1-F | Prithvi |
|---|---|---|---|
| 0–10° | 0.338 | 0.518 | 0.053 |
| 10–20° | 0.592 | 0.640 | 0.623 |
| 20–30° | 0.500 | 0.546 | 0.525 |
| > 30° | 0.374 | 0.400 | 0.359 |

Разбор задним числом (не решает):
- **Гарь на склонах не теряется**: полнота 0.94–0.97 у всех трёх моделей на всех
  уклонах, включая > 30°. Пропусков 43–50 тыс. пикселей из 1.06 млн.
- **Ошибка — ложные тревоги.** У Prithvi 5.8 млн ложных пикселей, из них 3.1 млн на
  море и 2.2 млн на равнине; у C1-F — 0.75 млн. Без воды: C1-F 0.601, C1 0.517,
  Prithvi 0.268.
- **На крутых склонах падает точность** (> 30°: 0.37–0.41 против 0.61–0.66 на
  10–20°). Вероятно, это тени рельефа, но не проверено.
- Оговорка: FLOGA размечает только гари того же года. Старые гари 2017–2019 в окне
  числятся фоном, так что часть «ложных» может быть настоящей гарью.

Выводы для гор. (1) Нулевой выстрел Prithvi в Средиземноморье непригоден без маски
воды. (2) Наш U-Net переносится лучше, а с добавкой разнообразных данных (C1-F) ещё
лучше — тот же вывод, что SPEC-75. (3) Следующие рычаги — маска воды (бит 5 Fmask) и
поправка на освещённость склона по DEM и углам солнца (SZA/SAA есть в HLS). Каждый —
отдельная спека с проверкой на других окнах.

Не сделано: Кипр и Испания. Открытых контуров гарей не нашли, EFFIS выдаёт их по
заявке. Когда появятся контуры, сборщик `build_floga_hls.py` переносится на них почти
без изменений.
