# {{ID}}: {{TITLE}}

## Summary

One paragraph: the product/engineering pillar this requirement charters.
Requirements are BROAD and few (one per durable pillar, not per feature) —
fine-grained theming lives in specs/index.md ranges. See
requirements/README.md.

## Motivation

Why this pillar exists and what breaks without it.

## Scope

- What is inside this pillar (subsystems, layers, workloads).
- What is explicitly outside it (and which REQ owns that instead).

## Acceptance Criteria

- High-level, durable criteria — things that stay true across many specs
  (e.g. "every optimizer rewrite is backed by a proof or a receipt"), not
  per-feature checklists.
